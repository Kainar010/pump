import styled from 'styled-components';

export const IntroduceStyledBox = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;

	height: inherit;

	.logo {
		width: 200px;
		height: 200px;
		margin-bottom: 25px;

		object-fit: cover;
	}
`;
